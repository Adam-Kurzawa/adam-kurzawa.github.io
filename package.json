{
  "name": "personal-website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "errorpage": "copyfiles index.html ./temp && cd ./temp && renamer index.html --replace 404.html && cd ..",
    "build": "npm run errorpage && vite build",
    "preview": "npm run build && vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "vue": "^3.4.21",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "copyfiles": "^2.4.1",
    "gh-pages": "^6.1.1",
    "renamer": "^5.0.0",
    "vite": "^5.1.6"
  }
}
